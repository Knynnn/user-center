<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yupi.usercenter.mapper.UserMapper">

    <!-- 查询所有用户 -->
    <select id="selectAllUsers" resultType="com.yupi.usercenter.model.domain.User">
        SELECT id, username, userAccount, avatarUrl, gender, userPassword, phone, email,
               userStatus, createTime, updateTime, isDelete, userRole
        FROM user
        WHERE isDelete = 0
    </select>

    <!-- 根据账号查询用户 -->
    <select id="selectByAccount" parameterType="String" resultType="com.yupi.usercenter.model.domain.User">
        SELECT id, username, userAccount, avatarUrl, gender, userPassword, phone, email,
               userStatus, createTime, updateTime, isDelete, userRole
        FROM user
        WHERE userAccount = #{userAccount}
          AND isDelete = 0
        LIMIT 1
    </select>

    <!-- 更新头像 -->
    <update id="updateAvatar" parameterType="map">
        UPDATE user
        SET avatarUrl = #{avatarUrl}, updateTime = NOW()
        WHERE id = #{id}
    </update>

</mapper>
